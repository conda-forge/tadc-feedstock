schema_version: 1

context:
  version: "2.4.2"

package:
  name: tadc
  version: ${{ version }}

source:
  url: https://github.com/NOAA-CO-OPS/CO-OPS-Tidal-Analysis-Datum-Calculator/archive/v${{ version }}.tar.gz
  sha256: d0926ac34fc47e775aa8f9289330d44ffa385f0907486c93655d269f3b858c49

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61.0
    - pip
  run:
    - python >=${{ python_min }}
    - matplotlib-base
    - numpy
    - pandas >=2.0.3
    - python-dateutil
    - requests
    - scipy

tests:
  - python:
      imports:
        - tadc
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  homepage: https://github.com/NOAA-CO-OPS/CO-OPS-Tidal-Analysis-Datum-Calculator
  summary: The Tidal Analysis Datum Calculator (TADC) computes tidal datums from a water level data time series.
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - ocefpaf
